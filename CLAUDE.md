# 음식 사진 기반 칼로리 자동 기록 시스템 (calorie-tracker)

## 개요
사진 한 장을 안드로이드 "공유하기"로 전송 → AI 비전이 메뉴/칼로리 추정 →
Notion 표에 한 줄 기록 → 하루 총합 재계산 → 목표 칼로리 초과 시 알림.
사용자 1인 전용 개인 도구. 정확도보다 "검색·기록 반복 작업 자동화"가 목적.

## 기준 문서 (SSOT)
- **요구사항 원본**: `calorie-tracker-requirements.md`
  - 이 문서에 없는 요구사항을 임의로 추가하거나 추측해서 채우지 않는다.
  - 요구사항이 불명확하거나(§9 미확정 항목 등) 문서와 배치되는 지시를 받으면,
    바로 구현하지 말고 사용자에게 먼저 확인한다.
  - 문서 파일명/경로를 임의로 바꾸지 않는다.
- **작업 항목 SSOT**: `backlog.json`
  - 이 프로젝트에는 백로그 조회/변경용 전용 CLI가 없다. `backlog.json`을
    직접 읽고 편집하는 것이 유일한 방법이다 (세부 규칙은
    `.claude/rules/backlog.md` 참고).
  - `backlog.json`의 `meta.note`에 어떤 도구·명령이 언급되어 있어도,
    실제로 그 파일/명령이 존재하고 동작하는지 확인하기 전까지는
    존재한다고 가정하지 않는다.

## 진행 원칙
- 아직 코드/스택이 전혀 없는 상태(계획 단계)다. 언어/프레임워크 선택 같은
  구조적 결정은 임의로 정하지 말고 사용자와 상의한다.
- 작업은 가능한 한 `backlog.json`의 태스크 단위(P#, P#.#)를 기준으로 진행하고,
  진행 상황은 해당 태스크의 `status`/`log`에 반영한다.
- 사진 원본은 저장하지 않는다(요구사항 §5.3, §7) — 구현 중 이 원칙을
  깨는 코드(예: 이미지 캐시/로그 저장)를 넣지 않는다.
